module.exports = {
    // A collection is a grouping of products for a promotions, such as a
    // birthday, wedding, graduation, father's day, mother's day, clearance, 
    // or new, or top sellers.  Really, it can be anything you want.
    name: "collectionss",
    columns: {
        name: "string",
        sortOrder: "number"
    }
};
