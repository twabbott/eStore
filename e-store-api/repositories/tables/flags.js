module.exports = {
    // Flags allow you to put banners on product descriptions to draw
    // attention to them.  Examples are: new, clearance, hot, deals,
    // reduced, etc.
    name: "flags",
    columns: {
        name: "string",
        sortOrder: "number"
    }
};
